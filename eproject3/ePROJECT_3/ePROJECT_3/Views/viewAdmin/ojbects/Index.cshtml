@model IEnumerable<ePROJECT_3.Models.ojbect>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ojbectName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ojbectName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ojbectID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ojbectID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ojbectID })
        </td>
    </tr>
}

</table>
