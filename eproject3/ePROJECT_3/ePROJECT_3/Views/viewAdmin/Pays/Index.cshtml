@model IEnumerable<ePROJECT_3.Models.Pay>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.payName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.payName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.payId }) |
            @Html.ActionLink("Details", "Details", new { id=item.payId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.payId })
        </td>
    </tr>
}

</table>
